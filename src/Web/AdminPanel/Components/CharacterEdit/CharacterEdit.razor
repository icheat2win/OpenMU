@using System.ComponentModel.DataAnnotations
@using MUnique.OpenMU.DataModel.Entities
@using MUnique.OpenMU.DataModel.Configuration
@using MUnique.OpenMU.Web.AdminPanel.Components.Form
@using MUnique.OpenMU.Persistence

<PageTitle>Edit Character: @Character.Name</PageTitle>

<style>
    /* Force dark theme for entire page */
    body {
        background-color: #0f172a !important;
        color: #f1f5f9 !important;
    }
    
    .character-edit-container {
        max-width: 1400px;
        margin: 0 auto;
        padding: 20px;
        background-color: #0f172a;
        min-height: 100vh;
    }
    
    .character-header {
        background: linear-gradient(135deg, #1e3a8a 0%, #7c3aed 100%);
        border: 1px solid rgba(255, 255, 255, 0.2);
        border-radius: 16px;
        padding: 24px;
        margin-bottom: 24px;
        box-shadow: 0 4px 6px rgba(0, 0, 0, 0.3);
    }
    
    .character-header h1 {
        font-size: 2rem;
        font-weight: 600;
        color: #f1f5f9;
        margin: 0 0 8px 0;
    }
    
    .character-header p {
        color: #94a3b8;
        margin: 0;
        font-size: 0.875rem;
    }
    
    .section-card {
        background: #1e293b;
        border: 1px solid rgba(255, 255, 255, 0.15);
        border-radius: 16px;
        padding: 24px;
        margin-bottom: 20px;
        box-shadow: 0 2px 8px rgba(0, 0, 0, 0.4);
    }
    
    .section-header {
        display: flex;
        align-items: center;
        gap: 8px;
        font-size: 1.125rem;
        font-weight: 600;
        color: #f1f5f9;
        margin-bottom: 20px;
        padding-bottom: 12px;
        border-bottom: 1px solid rgba(255, 255, 255, 0.1);
    }
    
    .section-icon {
        color: #06b6d4;
    }
    
    .form-grid {
        display: grid;
        grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
        gap: 20px;
    }
    
    .form-field {
        margin-bottom: 0;
    }
    
    .form-field label {
        display: block;
        font-size: 0.75rem;
        font-weight: 500;
        color: #94a3b8;
        text-transform: uppercase;
        letter-spacing: 0.05em;
        margin-bottom: 8px;
    }
    
    .form-field input,
    .form-field select,
    .form-field .form-control {
        width: 100% !important;
        padding: 12px 16px !important;
        background: rgba(0, 0, 0, 0.3) !important;
        border: 1px solid rgba(255, 255, 255, 0.1) !important;
        border-radius: 12px !important;
        color: #f1f5f9 !important;
        font-size: 0.875rem !important;
        transition: all 0.2s !important;
        height: auto !important;
    }
    
    .form-field input:focus,
    .form-field select:focus,
    .form-field .form-control:focus {
        outline: none !important;
        border-color: rgba(6, 182, 212, 0.6) !important;
        box-shadow: 0 0 0 3px rgba(6, 182, 212, 0.1) !important;
        background: rgba(0, 0, 0, 0.4) !important;
    }
    
    /* Override Bootstrap placeholder color */
    .form-field input::placeholder,
    .form-field .form-control::placeholder {
        color: #64748b !important;
        opacity: 1 !important;
    }
    
    /* Style BlazoredTypeahead dropdowns */
    .form-field .blazored-typeahead__input {
        width: 100% !important;
        padding: 12px 16px !important;
        background: rgba(0, 0, 0, 0.3) !important;
        border: 1px solid rgba(255, 255, 255, 0.1) !important;
        border-radius: 12px !important;
        color: #f1f5f9 !important;
        font-size: 0.875rem !important;
    }
    
    .form-field .blazored-typeahead__input:focus {
        outline: none !important;
        border-color: rgba(6, 182, 212, 0.6) !important;
        box-shadow: 0 0 0 3px rgba(6, 182, 212, 0.1) !important;
        background: rgba(0, 0, 0, 0.4) !important;
    }
    
    .form-field small {
        display: block;
        margin-top: 6px;
        font-size: 0.75rem;
        color: #64748b;
    }
    
    .checkbox-field {
        display: flex;
        align-items: center;
        gap: 8px;
        padding: 12px 16px;
        background: rgba(0, 0, 0, 0.2);
        border: 1px solid rgba(255, 255, 255, 0.1);
        border-radius: 12px;
    }
    
    .checkbox-field input[type="checkbox"] {
        width: auto;
        margin: 0;
    }
    
    .checkbox-field label {
        margin: 0;
        font-size: 0.875rem;
        color: #e2e8f0;
        text-transform: none;
        letter-spacing: normal;
    }
    
    .action-buttons {
        display: flex;
        gap: 12px;
        justify-content: flex-end;
        margin-top: 24px;
    }
    
    .btn-save {
        background: linear-gradient(135deg, #06b6d4 0%, #3b82f6 100%);
        border: none;
        color: white;
        padding: 12px 32px;
        border-radius: 12px;
        font-weight: 600;
        font-size: 0.875rem;
        cursor: pointer;
        transition: all 0.2s;
        box-shadow: 0 4px 12px rgba(6, 182, 212, 0.25);
    }
    
    .btn-save:hover {
        transform: translateY(-1px);
        box-shadow: 0 6px 16px rgba(6, 182, 212, 0.35);
        filter: brightness(1.1);
    }
    
    .btn-cancel {
        background: transparent;
        border: 1px solid rgba(255, 255, 255, 0.1);
        color: #cbd5e1;
        padding: 12px 24px;
        border-radius: 12px;
        font-weight: 600;
        font-size: 0.875rem;
        cursor: pointer;
        transition: all 0.2s;
    }
    
    .btn-cancel:hover {
        background: rgba(255, 255, 255, 0.05);
        border-color: #64748b;
    }
    
    .stats-grid {
        display: grid;
        grid-template-columns: repeat(auto-fill, minmax(150px, 1fr));
        gap: 16px;
    }

    /* Inventory Styles */
    .inventory-section {
        display: flex;
        flex-direction: column;
        gap: 20px;
    }

    .inventory-stats {
        display: grid;
        grid-template-columns: repeat(auto-fit, minmax(150px, 1fr));
        gap: 12px;
        padding: 16px;
        background: rgba(0, 0, 0, 0.2);
        border-radius: 12px;
    }

    .stat-badge {
        display: flex;
        flex-direction: column;
        gap: 4px;
    }

    .stat-badge .stat-label {
        font-size: 0.75rem;
        color: #94a3b8;
        text-transform: uppercase;
        letter-spacing: 0.05em;
    }

    .stat-badge .stat-value {
        font-size: 1.5rem;
        font-weight: 700;
        color: #06b6d4;
    }

    .inventory-grid {
        display: flex;
        flex-direction: column;
        gap: 8px;
    }

    .inventory-header {
        display: grid;
        grid-template-columns: 2fr 1fr 1fr 1.5fr 100px;
        gap: 12px;
        padding: 12px 16px;
        background: rgba(0, 0, 0, 0.3);
        border-radius: 8px;
        font-size: 0.75rem;
        font-weight: 600;
        color: #94a3b8;
        text-transform: uppercase;
        letter-spacing: 0.05em;
    }

    .inventory-item {
        display: grid;
        grid-template-columns: 2fr 1fr 1fr 1.5fr 100px;
        gap: 12px;
        padding: 12px 16px;
        background: rgba(0, 0, 0, 0.2);
        border: 1px solid rgba(255, 255, 255, 0.1);
        border-radius: 8px;
        align-items: center;
        transition: all 0.2s;
    }

    .inventory-item:hover {
        background: rgba(0, 0, 0, 0.3);
        border-color: rgba(6, 182, 212, 0.5);
        transform: translateX(4px);
    }

    .item-name {
        display: flex;
        align-items: center;
        gap: 8px;
        font-weight: 500;
    }

    .item-icon {
        font-size: 1.2rem;
    }

    .item-level .badge {
        font-size: 0.875rem;
        padding: 4px 8px;
    }

    .item-slot {
        color: #94a3b8;
        font-size: 0.875rem;
    }

    .item-options .badge {
        font-size: 0.75rem;
        padding: 4px 8px;
    }

    .item-actions {
        display: flex;
        gap: 8px;
        justify-content: center;
    }

    .btn-icon {
        padding: 6px 10px;
        border: none;
        border-radius: 6px;
        cursor: pointer;
        transition: all 0.2s;
        font-size: 1rem;
        background: rgba(255, 255, 255, 0.1);
    }

    .btn-icon:hover {
        transform: scale(1.1);
    }

    .btn-danger-sm {
        background: rgba(239, 68, 68, 0.2);
        border: 1px solid rgba(239, 68, 68, 0.3);
    }

    .btn-danger-sm:hover {
        background: rgba(239, 68, 68, 0.4);
        border-color: rgba(239, 68, 68, 0.6);
    }

    .empty-state {
        text-align: center;
        padding: 48px 24px;
        color: #64748b;
    }

    .empty-icon {
        font-size: 4rem;
        display: block;
        margin-bottom: 16px;
        opacity: 0.5;
    }

    .empty-state p {
        margin: 0;
        font-size: 1.125rem;
    }

    .inventory-actions {
        display: flex;
        justify-content: center;
        padding-top: 12px;
    }

    .btn-add-item {
        background: linear-gradient(135deg, #06b6d4 0%, #0891b2 100%);
        color: white;
        box-shadow: 0 4px 14px rgba(6, 182, 212, 0.3);
        padding: 14px 32px;
        border: none;
        border-radius: 12px;
        font-weight: 600;
        font-size: 1rem;
        cursor: pointer;
        transition: all 0.2s;
        display: flex;
        align-items: center;
        gap: 8px;
    }

    .btn-add-item:hover {
        transform: translateY(-2px);
        box-shadow: 0 6px 20px rgba(6, 182, 212, 0.4);
    }

    .badge {
        display: inline-block;
        padding: 4px 12px;
        border-radius: 6px;
        font-size: 0.75rem;
        font-weight: 600;
        text-transform: uppercase;
        letter-spacing: 0.05em;
    }

    .badge-info {
        background: rgba(6, 182, 212, 0.2);
        color: #06b6d4;
        border: 1px solid rgba(6, 182, 212, 0.3);
    }

    .badge-success {
        background: rgba(16, 185, 129, 0.2);
        color: #10b981;
        border: 1px solid rgba(16, 185, 129, 0.3);
    }

    .text-muted {
        color: #64748b;
    }
</style>

<div class="character-edit-container">
    <div class="character-header">
        <h1>✏️ Edit Character</h1>
        <p>@Character.Name • Level @(Character.Attributes?.FirstOrDefault(a => a.Definition?.Designation == "Level")?.Value ?? 0)</p>
    </div>

    <EditForm Model="Character" OnValidSubmit="OnValidSubmit">
        <DataAnnotationsValidator />
        
        <!-- Basic Information -->
        <div class="section-card">
            <div class="section-header">
                <span class="section-icon">📋</span>
                <span>Basic Information</span>
            </div>
            
            <div class="form-grid">
                <div class="form-field">
                    <label>Character Name</label>
                    <InputText @bind-Value="@Character.Name" />
                    <ValidationMessage For="@(() => Character.Name)" />
                </div>

                <div class="form-field">
                    <label>Character Class</label>
                    <LookupField @bind-Value="@Character.CharacterClass" />
                    <ValidationMessage For="@(() => Character.CharacterClass)" />
                </div>

                <div class="form-field">
                    <label>Character Slot</label>
                    <InputByte @bind-Value="@Character.CharacterSlot" Min="0" Max="4" />
                    <ValidationMessage For="@(() => Character.CharacterSlot)" />
                </div>
            </div>
        </div>
        
        <!-- Location & Position -->
        <div class="section-card">
            <div class="section-header">
                <span class="section-icon">🗺️</span>
                <span>Location & Position</span>
            </div>
            
            <div class="form-grid">
                <div class="form-field">
                    <label>Current Map</label>
                    <LookupField @bind-Value="@Character.CurrentMap" />
                    <ValidationMessage For="@(() => Character.CurrentMap)" />
                </div>

                <div class="form-field">
                    <label>Position X</label>
                    <InputByte @bind-Value="@Character.PositionX" Min="0" Max="255" />
                    <ValidationMessage For="@(() => Character.PositionX)" />
                </div>

                <div class="form-field">
                    <label>Position Y</label>
                    <InputByte @bind-Value="@Character.PositionY" Min="0" Max="255" />
                    <ValidationMessage For="@(() => Character.PositionY)" />
                </div>
            </div>
        </div>

        <!-- Experience & Level -->
        <div class="section-card">
            <div class="section-header">
                <span class="section-icon">⚡</span>
                <span>Experience & Level</span>
            </div>
            
            <div class="form-grid">
                <div class="form-field">
                    <label>Level</label>
                    <InputNumber @bind-Value="@CurrentLevel" min="1" max="400" />
                    <small>Character level (1-400)</small>
                </div>

                <div class="form-field">
                    <label>Experience</label>
                    <InputNumber @bind-Value="@Character.Experience" min="0" />
                    <ValidationMessage For="@(() => Character.Experience)" />
                </div>

                <div class="form-field">
                    <label>Level Up Points</label>
                    <InputNumber @bind-Value="@Character.LevelUpPoints" min="0" />
                    <ValidationMessage For="@(() => Character.LevelUpPoints)" />
                </div>

                <div class="form-field">
                    <label>Master Level</label>
                    <InputNumber @bind-Value="@CurrentMasterLevel" min="0" max="400" />
                    <small>Master level (0-400)</small>
                </div>

                <div class="form-field">
                    <label>Master Experience</label>
                    <InputNumber @bind-Value="@Character.MasterExperience" min="0" />
                    <ValidationMessage For="@(() => Character.MasterExperience)" />
                </div>

                <div class="form-field">
                    <label>Master Level Up Points</label>
                    <InputNumber @bind-Value="@Character.MasterLevelUpPoints" min="0" />
                    <ValidationMessage For="@(() => Character.MasterLevelUpPoints)" />
                </div>
            </div>
        </div>

        <!-- Character Stats -->
        <div class="section-card">
            <div class="section-header">
                <span class="section-icon">💪</span>
                <span>Character Stats</span>
            </div>
            
            <div class="form-grid">
                <div class="form-field">
                    <label>Strength</label>
                    <InputNumber @bind-Value="@Strength" min="0" max="65000" />
                    <small>Base strength attribute</small>
                </div>

                <div class="form-field">
                    <label>Agility</label>
                    <InputNumber @bind-Value="@Agility" min="0" max="65000" />
                    <small>Base agility attribute</small>
                </div>

                <div class="form-field">
                    <label>Vitality</label>
                    <InputNumber @bind-Value="@Vitality" min="0" max="65000" />
                    <small>Base vitality attribute</small>
                </div>

                <div class="form-field">
                    <label>Energy</label>
                    <InputNumber @bind-Value="@Energy" min="0" max="65000" />
                    <small>Base energy attribute</small>
                </div>

                <div class="form-field">
                    <label>Command</label>
                    <InputNumber @bind-Value="@Command" min="0" max="65000" />
                    <small>Command points for summoner</small>
                </div>
            </div>
        </div>

        <!-- Money & Currency -->
        <div class="section-card">
            <div class="section-header">
                <span class="section-icon">💰</span>
                <span>Money & Currency</span>
            </div>
            
            <div class="form-grid">
                <div class="form-field">
                    <label>Inventory Zen (Money)</label>
                    <InputNumber @bind-Value="@InventoryMoney" min="0" />
                    <small>Current zen in character inventory</small>
                </div>

                <div class="form-field">
                    <label>Vault Zen (Bank)</label>
                    <InputNumber @bind-Value="@VaultMoney" min="0" />
                    <small>Zen stored in account vault</small>
                </div>
            </div>
        </div>

        <!-- Character State -->
        <div class="section-card">
            <div class="section-header">
                <span class="section-icon">💫</span>
                <span>Character State</span>
            </div>
            
            <div class="form-grid">
                <div class="form-field">
                    <label>Hero State</label>
                    <InputSelect @bind-Value="@Character.State">
                        @foreach (var state in Enum.GetValues<HeroState>())
                        {
                            <option value="@state">@state</option>
                        }
                    </InputSelect>
                    <ValidationMessage For="@(() => Character.State)" />
                </div>

                <div class="form-field">
                    <label>State Remaining Seconds</label>
                    <InputNumber @bind-Value="@Character.StateRemainingSeconds" min="0" />
                    <ValidationMessage For="@(() => Character.StateRemainingSeconds)" />
                </div>

                <div class="form-field">
                    <label>Character Status</label>
                    <InputSelect @bind-Value="@Character.CharacterStatus">
                        @foreach (var status in Enum.GetValues<CharacterStatus>())
                        {
                            <option value="@status">@status</option>
                        }
                    </InputSelect>
                    <ValidationMessage For="@(() => Character.CharacterStatus)" />
                </div>

                <div class="form-field">
                    <label>Pose</label>
                    <InputSelect @bind-Value="@Character.Pose">
                        @foreach (var pose in Enum.GetValues<CharacterPose>())
                        {
                            <option value="@pose">@pose</option>
                        }
                    </InputSelect>
                    <ValidationMessage For="@(() => Character.Pose)" />
                </div>

                <div class="form-field">
                    <label>Player Kill Count</label>
                    <InputNumber @bind-Value="@Character.PlayerKillCount" min="0" />
                    <ValidationMessage For="@(() => Character.PlayerKillCount)" />
                </div>
            </div>
        </div>

        <!-- Fruit Points & Inventory -->
        <div class="section-card">
            <div class="section-header">
                <span class="section-icon">🎒</span>
                <span>Fruit Points & Inventory</span>
            </div>
            
            <div class="form-grid">
                <div class="form-field">
                    <label>Used Fruit Points</label>
                    <InputNumber @bind-Value="@Character.UsedFruitPoints" min="0" />
                    <ValidationMessage For="@(() => Character.UsedFruitPoints)" />
                </div>

                <div class="form-field">
                    <label>Used Negative Fruit Points</label>
                    <InputNumber @bind-Value="@Character.UsedNegFruitPoints" min="0" />
                    <ValidationMessage For="@(() => Character.UsedNegFruitPoints)" />
                </div>

                <div class="form-field">
                    <label>Inventory Extensions</label>
                    <InputNumber @bind-Value="@Character.InventoryExtensions" min="0" max="4" />
                    <small>Season 6+ feature (0-4 extensions)</small>
                    <ValidationMessage For="@(() => Character.InventoryExtensions)" />
                </div>
            </div>
        </div>

        <!-- Personal Store -->
        <div class="section-card">
            <div class="section-header">
                <span class="section-icon">🏪</span>
                <span>Personal Store</span>
            </div>
            
            <div class="form-grid">
                <div class="form-field">
                    <label>Store Name</label>
                    <InputText @bind-Value="@Character.StoreName" />
                    <ValidationMessage For="@(() => Character.StoreName)" />
                </div>

                <div class="checkbox-field">
                    <InputCheckbox @bind-Value="@Character.IsStoreOpened" />
                    <label>Store is Open</label>
                </div>
            </div>
        </div>

        <!-- Inventory Management -->
        <div class="section-card">
            <div class="section-header">
                <span class="section-icon">🎒</span>
                <span>Inventory & Equipment</span>
            </div>
            
            <div class="inventory-stats">
                <div class="stat-badge">
                    <span class="stat-label">Total Items:</span>
                    <span class="stat-value">@(Character.Inventory?.Items?.Count ?? 0)</span>
                </div>
                <div class="stat-badge">
                    <span class="stat-label">Current Zen:</span>
                    <span class="stat-value">@(Character.Inventory?.Money ?? 0)</span>
                </div>
                <div class="stat-badge">
                    <span class="stat-label">Extensions:</span>
                    <span class="stat-value">@Character.InventoryExtensions / 4</span>
                </div>
            </div>

            @if (Character.Inventory != null)
            {
                <ItemStorageField @bind-Value="@Character.Inventory" Label="Character Inventory" />
            }
            else
            {
                <div class="empty-state">
                    <span class="empty-icon">📭</span>
                    <p>No inventory storage available</p>
                    <button type="button" class="btn-add-item" @onclick="CreateInventory">
                        Create Inventory
                    </button>
                </div>
            }
        </div>

        <!-- Vault Storage -->
        @if (Account?.Vault != null)
        {
            <div class="section-card">
                <div class="section-header">
                    <span class="section-icon">🏦</span>
                    <span>Vault Storage</span>
                </div>
                
                <div class="inventory-stats">
                    <div class="stat-badge">
                        <span class="stat-label">Vault Items:</span>
                        <span class="stat-value">@(Account.Vault.Items?.Count ?? 0)</span>
                    </div>
                    <div class="stat-badge">
                        <span class="stat-label">Vault Zen:</span>
                        <span class="stat-value">@Account.Vault.Money</span>
                    </div>
                </div>

                <ItemStorageField @bind-Value="@Account.Vault" Label="Account Vault" />
            </div>
        }

        <!-- Other -->
        <div class="section-card">
            <div class="form-field">
                <label>Create Date</label>
                <InputDate @bind-Value="@Character.CreateDate" />
                <ValidationMessage For="@(() => Character.CreateDate)" />
            </div>
        </div>

        <ValidationSummary />
        
        <div class="action-buttons">
            @if (OnCancel.HasDelegate)
            {
                <button type="button" class="btn-cancel" @onclick="OnCancel">Cancel</button>
            }
            <button type="submit" class="btn-save">💾 Save Changes</button>
        </div>
    </EditForm>
</div>

@code {
    /// <summary>
    /// Gets or sets the character being edited.
    /// </summary>
    [Parameter]
    [Required]
    public Character Character { get; set; } = null!;

    /// <summary>
    /// Gets or sets the account that owns this character.
    /// </summary>
    [Parameter]
    public Account? Account { get; set; }

    /// <summary>
    /// Gets or sets the persistence context.
    /// </summary>
    [CascadingParameter]
    public IContext PersistenceContext { get; set; } = null!;

    /// <summary>
    /// Gets or sets the <see cref="EditForm.OnValidSubmit"/> event callback.
    /// </summary>
    [Parameter]
    public EventCallback OnValidSubmit { get; set; }

    /// <summary>
    /// Gets or sets the task which should be executed when the cancel button gets clicked.
    /// </summary>
    [Parameter]
    public EventCallback OnCancel { get; set; }

    /// <summary>
    /// Gets or sets the current character level.
    /// This property reads from and writes to the Character.Attributes collection.
    /// </summary>
    private int CurrentLevel
    {
        get
        {
            if (Character?.Attributes == null)
                return 1;
            
            var levelAttr = Character.Attributes.FirstOrDefault(a => a.Definition?.Designation == "Level");
            return levelAttr != null ? (int)levelAttr.Value : 1;
        }
        set
        {
            if (Character?.Attributes == null)
                return;
            
            var levelAttr = Character.Attributes.FirstOrDefault(a => a.Definition?.Designation == "Level");
            if (levelAttr != null)
            {
                levelAttr.Value = Math.Clamp(value, 1, 400);
            }
        }
    }

    /// <summary>
    /// Gets or sets the current character master level.
    /// This property reads from and writes to the Character.Attributes collection.
    /// </summary>
    private int CurrentMasterLevel
    {
        get
        {
            if (Character?.Attributes == null)
                return 0;
            
            var masterLevelAttr = Character.Attributes.FirstOrDefault(a => a.Definition?.Designation == "Master Level");
            return masterLevelAttr != null ? (int)masterLevelAttr.Value : 0;
        }
        set
        {
            if (Character?.Attributes == null)
                return;
            
            var masterLevelAttr = Character.Attributes.FirstOrDefault(a => a.Definition?.Designation == "Master Level");
            if (masterLevelAttr != null)
            {
                masterLevelAttr.Value = Math.Clamp(value, 0, 400);
            }
        }
    }

    /// <summary>
    /// Gets or sets the character strength attribute.
    /// Uses direct StatAttribute lookup by Definition ID.
    /// </summary>
    private int Strength
    {
        get
        {
            if (Character?.Attributes == null)
                return 0;
            
            // Use Guid ID: 123282fe-fead-448e-ad2c-baece939b4b1 for Base Strength
            var strengthAttr = Character.Attributes.FirstOrDefault(a => 
                a.Definition?.Id == Guid.Parse("123282fe-fead-448e-ad2c-baece939b4b1"));
            return strengthAttr != null ? (int)strengthAttr.Value : 0;
        }
        set
        {
            if (Character?.Attributes == null)
                return;
            
            var strengthAttr = Character.Attributes.FirstOrDefault(a => 
                a.Definition?.Id == Guid.Parse("123282fe-fead-448e-ad2c-baece939b4b1"));
            if (strengthAttr != null)
            {
                strengthAttr.Value = Math.Clamp(value, 0, 65000);
            }
        }
    }

    /// <summary>
    /// Gets or sets the character agility attribute.
    /// Uses direct StatAttribute lookup by Definition ID.
    /// </summary>
    private int Agility
    {
        get
        {
            if (Character?.Attributes == null)
                return 0;
            
            // Use Guid ID: 1ae9c014-e3cd-4703-bd05-1b65f5f94ceb for Base Agility
            var agilityAttr = Character.Attributes.FirstOrDefault(a => 
                a.Definition?.Id == Guid.Parse("1ae9c014-e3cd-4703-bd05-1b65f5f94ceb"));
            return agilityAttr != null ? (int)agilityAttr.Value : 0;
        }
        set
        {
            if (Character?.Attributes == null)
                return;
            
            var agilityAttr = Character.Attributes.FirstOrDefault(a => 
                a.Definition?.Id == Guid.Parse("1ae9c014-e3cd-4703-bd05-1b65f5f94ceb"));
            if (agilityAttr != null)
            {
                agilityAttr.Value = Math.Clamp(value, 0, 65000);
            }
        }
    }

    /// <summary>
    /// Gets or sets the character vitality attribute.
    /// Uses direct StatAttribute lookup by Definition ID.
    /// </summary>
    private int Vitality
    {
        get
        {
            if (Character?.Attributes == null)
                return 0;
            
            // Use Guid ID: 6ca5c3a6-b109-45a5-87a7-fdcb107b4982 for Base Vitality
            var vitalityAttr = Character.Attributes.FirstOrDefault(a => 
                a.Definition?.Id == Guid.Parse("6ca5c3a6-b109-45a5-87a7-fdcb107b4982"));
            return vitalityAttr != null ? (int)vitalityAttr.Value : 0;
        }
        set
        {
            if (Character?.Attributes == null)
                return;
            
            var vitalityAttr = Character.Attributes.FirstOrDefault(a => 
                a.Definition?.Id == Guid.Parse("6ca5c3a6-b109-45a5-87a7-fdcb107b4982"));
            if (vitalityAttr != null)
            {
                vitalityAttr.Value = Math.Clamp(value, 0, 65000);
            }
        }
    }

    /// <summary>
    /// Gets or sets the character energy attribute.
    /// Uses direct StatAttribute lookup by Definition ID.
    /// </summary>
    private int Energy
    {
        get
        {
            if (Character?.Attributes == null)
                return 0;
            
            // Use Guid ID: 01b0ef28-f7a0-46b5-97ba-2b624a54cd75 for Base Energy
            var energyAttr = Character.Attributes.FirstOrDefault(a => 
                a.Definition?.Id == Guid.Parse("01b0ef28-f7a0-46b5-97ba-2b624a54cd75"));
            return energyAttr != null ? (int)energyAttr.Value : 0;
        }
        set
        {
            if (Character?.Attributes == null)
                return;
            
            var energyAttr = Character.Attributes.FirstOrDefault(a => 
                a.Definition?.Id == Guid.Parse("01b0ef28-f7a0-46b5-97ba-2b624a54cd75"));
            if (energyAttr != null)
            {
                energyAttr.Value = Math.Clamp(value, 0, 65000);
            }
        }
    }

    /// <summary>
    /// Gets or sets the character command/leadership attribute.
    /// Uses direct StatAttribute lookup by Definition ID.
    /// </summary>
    private int Command
    {
        get
        {
            if (Character?.Attributes == null)
                return 0;
            
            // Use Guid ID: 6af2c9df-3ae4-4721-8462-9a8ec7f56fe4 for Base Leadership
            var commandAttr = Character.Attributes.FirstOrDefault(a => 
                a.Definition?.Id == Guid.Parse("6af2c9df-3ae4-4721-8462-9a8ec7f56fe4"));
            return commandAttr != null ? (int)commandAttr.Value : 0;
        }
        set
        {
            if (Character?.Attributes == null)
                return;
            
            var commandAttr = Character.Attributes.FirstOrDefault(a => 
                a.Definition?.Id == Guid.Parse("6af2c9df-3ae4-4721-8462-9a8ec7f56fe4"));
            if (commandAttr != null)
            {
                commandAttr.Value = Math.Clamp(value, 0, 65000);
            }
        }
    }

    /// <summary>
    /// Gets or sets the inventory money (Zen).
    /// </summary>
    private int InventoryMoney
    {
        get => Character?.Inventory?.Money ?? 0;
        set
        {
            if (Character?.Inventory != null)
            {
                Character.Inventory.Money = Math.Max(0, value);
            }
        }
    }

    /// <summary>
    /// Gets or sets the vault money (Bank Zen).
    /// Requires Character to have an Account with a Vault.
    /// </summary>
    private int VaultMoney
    {
        get => Account?.Vault?.Money ?? 0;
        set
        {
            if (Account?.Vault != null)
            {
                Account.Vault.Money = Math.Max(0, value);
            }
        }
    }

    /// <summary>
    /// Removes an item from the character's inventory.
    /// </summary>
    private void RemoveItem(Item item)
    {
        if (Character?.Inventory?.Items != null)
        {
            Character.Inventory.Items.Remove(item);
            StateHasChanged();
        }
    }

    /// <summary>
    /// Shows dialog to add a new item to inventory.
    /// </summary>
    private void ShowAddItemDialog()
    {
        if (Character?.Inventory != null && PersistenceContext != null)
        {
            var newItem = PersistenceContext.CreateNew<Item>();
            Character.Inventory.Items.Add(newItem);
            StateHasChanged();
        }
    }

    /// <summary>
    /// Creates a new inventory for the character if one doesn't exist.
    /// </summary>
    private void CreateInventory()
    {
        if (Character != null && PersistenceContext != null && Character.Inventory == null)
        {
            Character.Inventory = PersistenceContext.CreateNew<ItemStorage>();
            StateHasChanged();
        }
    }
}

