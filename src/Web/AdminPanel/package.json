{
  "name": "adminpanel",
  "version": "1.0.0",
  "description": "The admin panel is meant to offer functions for administrative tasks.",
  "main": "index.js",
  "scripts": {
    "build:css": "npx @tailwindcss/cli -i ./Styles/tailwind.css -o ./wwwroot/css/tailwind.min.css --minify",
    "watch:css": "npx @tailwindcss/cli -i ./Styles/tailwind.css -o ./wwwroot/css/tailwind.min.css --watch",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@tailwindcss/cli": "^4.0.0-beta.7",
    "tailwindcss": "^4.0.0-beta.7"
  }
}
